import { FourInRowMiddleware } from './four-in-row.middleware';

describe('FourInRowMiddleware', () => {
  it('should be defined', () => {
    expect(new FourInRowMiddleware()).toBeDefined();
  });
});
